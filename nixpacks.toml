[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx prisma db push --accept-data-loss && npx next start -p $PORT"
