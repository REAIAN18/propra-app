[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma db push --skip-generate", "npm run build"]

[start]
cmd = "npx next start -p $PORT"
