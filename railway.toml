[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "sh -c 'timeout 30 npx prisma db push --accept-data-loss || true; HOSTNAME=0.0.0.0 node .next/standalone/server.js'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
