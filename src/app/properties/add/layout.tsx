import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Add Property — RealHQ",
};

export default function AddPropertyLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
