{
  "buildCommand": "prisma generate && (prisma migrate resolve --rolled-back \"20260329233150_add_vendor_approach\" || true) && prisma migrate deploy && npm run type-check && npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "framework": "nextjs"
}
